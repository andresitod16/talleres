// Usa template strings (`) para crear un
// mensaje que diga: "El resultado de 5 + 7 es
// [resultado]" (calcula la suma dentro del
// string). 

let n1 = 5;
let n2 = 7;
let resultado = (n1+n2);
console.log(`la suma es de ${n1}+${n2} es ` + resultado );