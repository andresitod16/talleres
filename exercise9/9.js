// Pide al usuario 3 colores (usando prompt)
// y guárdalos en un array (colores). Luego,
// muéstralos con console.log.

let colores = (prompt("ingresa tus colores"));

let array = [];
array.push(colores);
console.log(colores);
